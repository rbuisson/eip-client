version: 1

build:
  bash_commands: "echo 'building...'"
deploy:
  bash_commands: "echo 'deploying...'"
  artifact:
    artifactId: "openmrs-eip-client"
    groupId: "net.mekomsolutions.eip"
    version: "1.0.0-SNAPSHOT"
